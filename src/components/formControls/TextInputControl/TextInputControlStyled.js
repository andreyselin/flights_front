import styled from 'styled-components';
import {basicLabelStyle, BasicStyledInput} from "../utilities/basicFormStyles";

export const TextInputLabelStyled = styled.label`
  ${basicLabelStyle}
`;

export const TextInputStyled = styled(BasicStyledInput)`
`;
