import styled from 'styled-components';
import {basicInputBorderedStyle, basicLabelStyle} from "../utilities/basicFormStyles";

export const NumberInputLabelStyled = styled.label`
  ${basicLabelStyle}
`;

export const NumberInputStyled = styled.input`
  ${ basicInputBorderedStyle }
`;
